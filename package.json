{
  "name": "my-angular-todo",
  "private": true,
  "version": "0.0.0",
  "description": "My todo app in AngularJS",
  "repository": "",
  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1",
    "shelljs": "^0.2.6"
  },
  "scripts": {
    "postinstall": "cd app/ && bower install -c",

    "prestart": "npm install",
    "start": "http-server -a localhost -p 8080 -c-1"
  }
}
